---
import cn from 'mxcn';

interface Props {
  invert?: boolean;
}

const { invert } = Astro.props as Props;
---

<span class={cn("relative transition-all duration-500 overflow-hidden px-4 py-2 w-fit rounded-full border border-black text-2xl font-['Poppins'] hover:text-white before:rounded-full before:bg-black before:absolute before:top-0 before:left-0 before:h-full before:w-0 before:transition-all before:duration-500 before:-z-10 before:content-[''] before:block hover:before:w-full", {
    'bg-black text-white text-lg before:bg-white hover:bg-transparent hover:text-black': invert})}>
    <slot />
</span>