---
import cn from 'mxcn';

interface Props {
  title: string;
  isReverse?: boolean;
  id?: string;
}

const { title, isReverse } = Astro.props as Props;
---
<section id={Astro.props.id} class={cn(
    'flex items-center justify-between px-4 md:px-8 py-4 md:py-8 h-full flex-col',
    isReverse ? 'md:flex-row-reverse' : 'md:flex-row',
    )}>
    <div class={cn(
        'title text-center md:w-1/3',
        isReverse ? 'md:text-right' : 'md:text-left',
    )}>
        <h2 class="text-6xl">{title}</h2>
    </div>
    <div class="vertical-line"></div>
    <div class="content md:w-2/3">
        <slot />
    </div>
</section>